<h1>Cats upon cats!</h1>

<% if flash[:success] %>
    <%= flash[:success] %>
<% else %>
  <%= flash[:errors] %>
<% end %>

<ul>
  <% @cats.each do |cat| %>
    <li><a href="<%= cat_url(cat) %>"><%= cat.name %></a></li>
  <% end %>
</ul>

<a href="<%= new_cat_url %>">Create cat</a>
